# DeepFin Trading Bot — dependencias (stack open-source, costo $0)
# Versiones como >= minimo para que Streamlit Cloud encuentre wheels compatibles
# y no aborte el install (los == exactos pueden no tener wheel en la Python de la nube).
# streamlit/plotly van primero: son criticos para el dashboard.
streamlit>=1.40
plotly>=5.24
pandas>=2.2
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
xgboost>=2.1
catboost>=1.2
statsmodels>=0.14
ccxt>=4.4
fastapi>=0.115
uvicorn>=0.32
pyyaml>=6.0
python-dotenv>=1.0
joblib>=1.4
pytest>=8.3
